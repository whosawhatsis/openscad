// Rendered by smoothrendertest.py in shaded mode, once per fa= value. $fn fixes the
// mesh, so the geometry is identical between runs and only the smoothing tolerance
// (2 * $fa) differs.
fa = 12;
$fa = fa;
sphere(20, $fn = 24);
